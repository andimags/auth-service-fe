/** Delay before refreshing the current route's data after a successful mutation, so the toast is visible first. */
export const REFRESH_DELAY_MS = 1000

/** Delay before navigating away after a successful delete, so the toast is visible first. */
export const NAVIGATE_AWAY_DELAY_MS = 1500

/** Delay before clearing an ARIA live-region announcement, so repeated identical announcements are re-read by screen readers. */
export const ARIA_ANNOUNCEMENT_CLEAR_DELAY_MS = 100
